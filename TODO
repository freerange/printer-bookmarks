* Add author to the bookmark

* Improve README

* Increase the size of the font used for the "Started", "Finished" and "Notes" headings

* Increase the length of the printout

* Use external stylesheet to avoid cluttering layout

* Use sass rather than plain old css